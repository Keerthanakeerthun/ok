#include<iostream>
#include<stdio.h>
 
using namespace std;
 
int main()
{
    char s[1000];
    int i,count=1;
    
    cout<<"Enter a string:";
    gets(s);
    
    for(i=0;a[i]!='\0';++i)
    {
        if(a[i]==' ')
            count++;
    }
    
    cout<<"The totalnumber of words in the given string is "<<count<<endl;
 
    return 0;
}
